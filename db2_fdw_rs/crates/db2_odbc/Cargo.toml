[package]
name = "db2_odbc"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Safe ODBC wrapper for DB2 database connections"

[features]
default = []
# Enable real ODBC connections - requires libodbc and fixes for Connection<'env> lifetimes
# TODO: Restructure to use owned connections or Arc<Environment>
real_odbc = []

[dependencies]
odbc-api.workspace = true
thiserror.workspace = true
tracing.workspace = true
chrono.workspace = true
rust_decimal.workspace = true
parking_lot = "0.12"

